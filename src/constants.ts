export enum Routes {
  LOGIN = '/login',
  REGISTER = '/register',
  DASHBOARD = '/dashboard',
  CATEGORIES = '/categories',
  PROFILE = '/profile',
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
}
