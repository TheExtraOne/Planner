export enum Routes {
  LOGIN = '/login',
  REGISTER = '/register',
  DASHBOARD = '/dashboard',
  PROJECT = '/project/:id',
  PROFILE = '/profile',
}

export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
}
